import React from 'react'

const PortionTitle = ({className,text}) => {
  return (
    <h4 className={`font-pop text-base font-normal text-four ${className}`}>{text}</h4>
  )
}

export default PortionTitle